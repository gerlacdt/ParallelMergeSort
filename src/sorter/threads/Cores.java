package sorter.threads;

/**
 * Helper class which should indicate with how many cores the MergeSorterDriver
 * should start the Mergesort.
 * At the moment one SINGLE, DUAL and QUAD-core is supported.
 * 
 * @author gerlacdt
 * 
 */
public enum Cores {

	SINGLE, DUAL, QUAD

}
